# 新人上手指南（当前仓库）

## 1. 仓库现状

当前仓库是一个**初始化完成但尚未落地业务代码**的骨架仓库：

- Git 历史只有一次初始化提交。
- 当前受版本管理的文件只有 `.gitkeep`（空文件，通常用于保留空目录或占位）。

这意味着：

- 还没有明确的语言栈（如 Node/Python/Go 等）。
- 还没有应用入口、模块划分、测试体系、CI/CD 配置。
- 也没有文档规范（如 README、贡献指南、架构说明）。

## 2. 整体结构怎么理解

目前从代码角度几乎没有结构，只有两层“隐式结构”可参考：

1. **Git 仓库结构**：`.git/` 目录记录版本控制元数据。
2. **业务代码占位结构**：`.gitkeep` 暗示未来会有目录或代码被补充。

因此，新人现在要建立的不是“读现有代码”，而是“共同定义项目结构与工程规范”。

## 3. 需要优先了解的关键内容（建议按顺序）

### A. 业务与目标
- 项目解决什么问题？
- 目标用户是谁？
- MVP（最小可用版本）范围是什么？

### B. 技术选型
- 后端/前端/数据层选择什么栈？
- 是否需要容器化、部署平台、云资源？
- 是否有安全与合规要求？

### C. 工程约定
- 分支策略（main/dev/feature）
- 提交规范（Conventional Commits 等）
- 代码风格、Lint、格式化、测试基线

### D. 交付流程
- CI 要做哪些检查（lint/test/build）
- 发布流程（tag、release、回滚）
- 监控与日志（后续运维必需）

## 4. 建议尽快补齐的基础文件

建议第一阶段新增以下文件（即便内容简短也很有帮助）：

- `README.md`：项目简介、启动方式、目录说明
- `CONTRIBUTING.md`：开发流程、提交规范、PR 要求
- `docs/architecture.md`：核心架构与模块边界
- `.editorconfig`：统一编辑器行为
- 对应语言的格式化/静态检查配置（如 `pyproject.toml`/`eslint`/`prettier`）
- CI 配置（如 GitHub Actions）

## 5. 给新人的学习建议（前 2~4 周）

### 第 1 周：补齐认知
- 先和项目负责人对齐业务目标与阶段里程碑。
- 参与技术选型讨论，明确“为什么这么选”。
- 输出一版项目骨架提案（目录+规范+CI）。

### 第 2 周：搭建工程底座
- 建立基础目录结构。
- 接入 lint/test/build 基线。
- 完成第一个可运行的 Hello API 或页面。

### 第 3~4 周：进入功能开发
- 按模块做小步迭代，保证每次提交都可验证。
- 与测试/产品建立固定评审节奏。
- 在开发中持续沉淀文档与最佳实践。

## 6. 推荐的最小目录模板（可按技术栈调整）

```text
.
├── README.md
├── CONTRIBUTING.md
├── docs/
│   └── architecture.md
├── src/
├── tests/
└── .github/
    └── workflows/
        └── ci.yml
```

---

如果你愿意，我下一步可以直接基于你们偏好的技术栈（例如 Python FastAPI / Node NestJS / Go Gin）生成一套可运行的初始工程骨架与 CI 配置。
